# تي دي للخدمات اللوجستية | TD Logistics

موقع إلكتروني متكامل لشركة تي دي للخدمات اللوجستية، مبني باستخدام Next.js 14 مع TypeScript وقاعدة بيانات SQLite.

## المميزات

- 🎨 تصميم عصري وأنيق باللغة العربية (RTL)
- 📦 تتبع الشحنات في الوقت الفعلي
- 🔐 نظام أمان متكامل (حماية من XSS, SQL Injection, CSRF)
- 👤 نظام تسجيل دخول وإدارة المستخدمين
- 📝 نظام إدارة المحتوى للمدونة
- 💼 صفحة الوظائف والتقديم
- 📧 نماذج التواصل وطلب عروض الأسعار
- 📱 تصميم متجاوب لجميع الأجهزة

## المتطلبات

- Node.js 18+
- npm أو yarn

## التثبيت

```bash
# تثبيت الحزم
npm install

# إعداد قاعدة البيانات
npm run db:setup

# تشغيل خادم التطوير
npm run dev
```

## إعداد البيئة

انسخ ملف `.env.example` إلى `.env.local` وعدّل القيم:

```bash
cp .env.example .env.local
```

**مهم:** غيّر قيمة `JWT_SECRET` في بيئة الإنتاج!

## بيانات الدخول الافتراضية

بعد تشغيل `npm run db:setup`:

- **اسم المستخدم:** admin
- **البريد:** info@tdlogistics.co
- **كلمة المرور:** Admin@123456

⚠️ **غيّر كلمة المرور فوراً في بيئة الإنتاج!**

## البنية

```
src/
├── app/                    # صفحات Next.js App Router
│   ├── api/               # API Routes
│   ├── about/             # صفحة من نحن
│   ├── blog/              # المدونة
│   ├── careers/           # الوظائف
│   ├── contact/           # اتصل بنا
│   ├── dashboard/         # لوحة التحكم
│   ├── login/             # تسجيل الدخول
│   ├── quote/             # طلب عرض سعر
│   ├── register/          # إنشاء حساب
│   ├── services/          # الخدمات
│   └── tracking/          # تتبع الشحنات
├── components/            # المكونات
│   ├── home/             # مكونات الصفحة الرئيسية
│   ├── layout/           # Header, Footer
│   └── ui/               # مكونات UI عامة
└── lib/                   # المكتبات والأدوات
    ├── db/               # قاعدة البيانات
    ├── security/         # أدوات الأمان
    └── validation/       # التحقق من البيانات
```

## الأمان

الموقع محمي ضد:

- ✅ XSS (Cross-Site Scripting)
- ✅ SQL Injection
- ✅ CSRF (Cross-Site Request Forgery)
- ✅ Rate Limiting
- ✅ Brute Force Attacks
- ✅ Session Hijacking
- ✅ Clickjacking

### إجراءات الأمان المطبقة:

1. **تنظيف المدخلات:** جميع المدخلات يتم تنظيفها قبل المعالجة
2. **استعلامات معلمة:** استخدام Prepared Statements لمنع SQL Injection
3. **تشفير كلمات المرور:** bcrypt مع 12 جولة
4. **JWT آمن:** توقيع HS256 مع انتهاء صلاحية
5. **Rate Limiting:** حد للطلبات لمنع الإساءة
6. **Security Headers:** CSP, HSTS, X-Frame-Options, etc.
7. **قفل الحساب:** بعد 5 محاولات فاشلة

## API التتبع

يستخدم الموقع API خارجي لتتبع الشحنات:

```
GET /api/tracking?barcode={barcode}
```

أمثلة أرقام تتبع للاختبار:
- `TDL100378616773` - جاهزة للاستلام
- `TDL100378630889` - خارجة للتوصيل
- `TDL100378632203` - تم التوصيل

## النشر

```bash
# بناء للإنتاج
npm run build

# تشغيل الإنتاج
npm start
```

## الترخيص

جميع الحقوق محفوظة © 2024 تي دي للخدمات اللوجستية

Developer: Muaadh AL-Sway
Studio: MUSTUDIOLAB
Last Updated: February 2026
